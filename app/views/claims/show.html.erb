I'm a page to display details about a particular claim thats been made on a donation


Yay for <%= @claim.charity %>!
You have claimed a donation from <%= @claim.business %>

This donation includes:
<ul>
<%  @claim.food_donation.food_items.each do | food_item | %>
  <li>food_item.name</li>
<% end %>
</ul>

<%= render review_partial if current_user.charity? %>
<%= render no_show_partial if current_user.business? %>

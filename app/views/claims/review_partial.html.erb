Leave a review:
  <%= simple_form_for [@claim, @review] do |f|%>
    <%= f.input :star, 'Stars', as: :radio_buttons, collection: [ 1, 2, 3, 4, 5] %>
    <%= f.input :content %>
    <%= f.button :submit, 'Submit Review' %>
  <% end %>

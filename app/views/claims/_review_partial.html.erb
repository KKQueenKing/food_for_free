Leave a review:
  <%= simple_form_for [@claim, Review.new] do |f|%>
    <%= f.collection_radio_buttons :star, 'Stars', as: :radio_buttons [0, 1, 2, 3, 4, 5] %>
    <%= f.input :content %>
    <%= f.button :submit, 'Submit Review' %>
  <% end %>

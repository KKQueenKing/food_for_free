<nav>
  <div class="navbar fixed-top navbar-expand-sm navbar-light border-bottom border-info">
      <div class="navbar_logo d-flex">
        <%= link_to image_tag("FFF_logo.png"), root_path %>
        </div>
      <div class="d-none d-md-block full-logo">
          <%= link_to image_tag("FFF_draft_full_logo.png"), root_path %>
      </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <%= image_tag "FFF_menu.png", crop: :fill, height: 68, width: 68  %>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active">

          <%= (link_to "Find a Donation", food_donations_path, class: "nav-link") if (user_signed_in? && current_user.charity)  %>
        </li>
        <li class="nav-item active">
          <%= link_to "Make a Donation", new_food_donation_path, class: "nav-link" if (user_signed_in? && current_user.business) %>
        </li>
        <li class="nav-item dropdown">
          <%= image_tag "FFF_logo_yellow.png", class: "dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">

            <%= link_to "My Profile", my_profile_path, class: "dropdown-item" if user_signed_in? %>
            <%= link_to "Register", new_user_registration_path, class: "nav-link" unless user_signed_in? %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link" if user_signed_in? %>
            <%= link_to "Sign In", user_session_path, class: "nav-link" unless user_signed_in? %>

          </div>
        </li>
      </ul>
    </div>

</nav>

